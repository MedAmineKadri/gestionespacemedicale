{% extends 'base.html.twig' %}

{% block title %}Dos{% endblock %}

{% block body %}
    <h1>Dos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ do.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ do.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ do.lastname }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ do.image }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ do.date }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dos_index') }}">back to list</a>

    <a href="{{ path('dos_edit', {'id': do.id}) }}">edit</a>

    {{ include('dos/_delete_form.html.twig') }}
{% endblock %}
