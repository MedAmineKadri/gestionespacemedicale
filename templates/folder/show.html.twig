{% extends 'base.html.twig' %}

{% block title %}Folder{% endblock %}

{% block body %}
    <h1>Folder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ folder.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ folder.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ folder.lastname }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ folder.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('folder_index') }}">back to list</a>

    <a href="{{ path('folder_edit', {'id': folder.id}) }}">edit</a>

    {{ include('folder/_delete_form.html.twig') }}
{% endblock %}
